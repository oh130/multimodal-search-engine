===== 남은 문제 목록 =====
기준일: 2026-04-29  (docker-compose 동작 확인: 2026-04-29)

────────────────────────────
[검색 엔진]
────────────────────────────
1. 이미지 검색 미검증
   - H&M 이미지 파일 없이도 CLIP cross-modal 검색으로 이미지 API 동작 가능
   - 임의 샘플 이미지 1장으로 image / hybrid 엔드포인트 동작 확인 필요
   - 30GB 전체 다운로드는 불필요 (명세서 필수 아님)

2. 검색 성능 지표 미측정
   - MRR ≥ 0.55, NDCG@10 ≥ 0.50 달성 여부 확인 안 됨
   - evaluation/metrics.py 스크립트로 테스트셋 기준 측정 필요

────────────────────────────
[추천 모델]
────────────────────────────
※ 참고: candidate_service, ranking_service, rerank_bridge, recommend_service는
  모두 실제 동작 코드가 구현되어 있음. MAB, 다양성 제어, 신상품 노출도 포함.
  다만 딥러닝 모델이 스켈레톤이라 현재는 sklearn 기반 baseline으로 동작 중.

3. Two-Tower 딥러닝 모델 부분 구현
   - candidate/model.py, train.py, dataset.py, infer.py, evaluator.py 구현 완료
   - 학습/추론/오프라인 평가는 가능하고 체크포인트(two_tower.pt)도 생성됨
   - 다만 serving candidate_service에는 아직 미연결이라 현재 서빙은 메타데이터 + 인기도 baseline으로 동작
   - Recall@300 측정됨: baseline 0.251665, Two-Tower 0.175214 (2026-04-12 실험 기준)
   - 목표 Recall@300 ≥ 0.30은 아직 미달

4. DeepFM Ranking 모델 미구현
   - ranking/model.py 스켈레톤만 있음
   - 현재는 sklearn LogisticRegression baseline으로 대체 중 (ranking_baseline.joblib)
   - ranking/train.py, infer.py, evaluator.py는 baseline 기준으로 구현 완료
   - AUC 측정됨: 0.947862 (2026-04-11 baseline 리포트, max_users=30 기준)
   - DeepFM/Wide&Deep 계열 실제 딥러닝 랭커는 아직 없음

5. 세션 인코더 미구현
   - session/session_encoder.py 스켈레톤만 있음
   - 현재 세션 관심사는 Redis에서 단순 키워드 매칭으로 처리 중
   - GRU/Transformer 기반 단기 관심 임베딩 없음

6. 추천 성능 지표 재측정 필요
   - baseline 리포트 생성 완료 (max_users=30 샘플 기준):
     HitRate@50 = 1.000000
     NDCG@50 = 0.933807
     Coverage@50 = 0.014961  ← 목표 0.20 미달
   - 전체 데이터 기준 재측정 필요


────────────────────────────
[문서화]
────────────────────────────
8. docs/ 실험 결과 리포트 없음
   - 검색 성능 측정 후 리포트 작성 필요
   - 추천 성능 전체 데이터 재측정 후 리포트 작성 필요
   - 명세서 필수 항목: 데이터 분할 방식, 지표 정의, 재현 시드, 베이스라인 대비 개선율 포함

────────────────────────────
[해결 완료]
────────────────────────────
✅ docker-compose up — 8개 컨테이너 전체 정상 동작 확인 (2026-04-29)
     redis, api-gateway, search-engine, rec-models,
     frontend, dashboard, simulator, ct-pipeline
✅ CT 파이프라인 구현 완료
     - ct_pipeline/monitor.py: 성능 모니터링, 임계값 알림, 재학습 트리거, 버전 관리
     - ct_pipeline/config.yaml: 임계값 설정 파일 (수정 가능)
     - docker-compose 컨테이너 등록 완료
     - api_gateway에 전역 이벤트 카운터(ct:event_count) 연동 완료
✅ docker-compose 컨테이너 누락 해결
     - frontend(:3000), dashboard(:8501), simulator, ct-pipeline 추가 완료
     - 각 Dockerfile 생성 완료
✅ README.md 아키텍처 다이어그램 + 실행 방법 + API 예시 + 팀 역할 작성 완료
✅ session_interest Redis → rec-models 미전달 버그 수정 완료
✅ data_pipeline 파일명 불일치 해결 (build_article_features.py 재실행)
✅ CLIP 사전학습 모델 교체 (openai/clip-vit-base-patch32) — HTTP 200 확인
✅ MAB (Epsilon-Greedy) → rerank_bridge.py 구현 완료
✅ Re-ranking 다양성 제어 (동일 카테고리 연속 3개 제한) → 구현 완료
✅ 신규 상품 노출 보장 로직 → 구현 완료
✅ A/B 테스트 통계 검정 (p-value, 신뢰구간) → evaluation/ab_test.py 구현 완료
✅ 오프라인 평가 메트릭 (HitRate, MRR, NDCG) → evaluation/metrics.py 구현 완료
✅ 프론트엔드 React 앱 → pront/ 구현 완료, docker-compose 컨테이너 추가 완료
✅ 추천 결과 Redis 캐싱 → api_gateway /api/recommend에 TTL 300s 캐시 구현 완료
     캐시 키: cache:recommend:{user_id}:{top_n}:{click_count} (클릭 발생 시 자동 무효화)
✅ simulator/ 구현 완료 — docker-compose 정상 동작 확인 (2026-04-29)
     - 6가지 페르소나(트렌드세터/실용주의자/가성비추구/브랜드충성/충동구매/신중탐색) 정의
     - search → click → cart → purchase 상태 머신 구현
     - /api/events로 이벤트 실시간 전송 (ct:event_count 자동 누적)
     - config.yaml으로 페르소나 비율, 전이 확률, 딜레이 변경 가능
     - API Gateway 기동 대기 로직 포함 (docker-compose 순서 무관하게 동작)
