===== 남은 문제 목록 (수정본) =====
기준일: 2026-04-11

────────────────────────────
[검색 엔진]
────────────────────────────
1. 이미지 검색 미검증
   - H&M 이미지 파일(images/ 폴더)이 없어 image / hybrid 검색을 실제 테스트 못함
   - Kaggle에서 images/ 폴더 추가 다운로드 필요 (약 30GB)
   - 또는 임의 샘플 이미지로 동작 여부만 확인

2. 검색 성능 지표 미측정
   - MRR ≥ 0.55, NDCG@10 ≥ 0.50 달성 여부 확인 안 됨
   - evaluation/metrics.py 스크립트로 테스트셋 기준 측정 필요

────────────────────────────
[추천 모델]
────────────────────────────
※ 참고: candidate_service, ranking_service, rerank_bridge, recommend_service는
  모두 실제 동작 코드가 구현되어 있음. MAB, 다양성 제어, 신상품 노출도 포함.
  다만 딥러닝 모델이 스켈레톤이라 현재는 sklearn 기반 baseline으로 동작 중.

3. Two-Tower 딥러닝 모델 미구현
   - candidate/model.py, train.py, dataset.py 모두 docstring 스켈레톤만 있음
   - 현재는 메타데이터 기반 유사도 매칭 + 인기도 폴백으로 대체 중
   - Recall@300 ≥ 0.30 달성 여부 미측정

4. DeepFM Ranking 모델 미구현
   - ranking/model.py 스켈레톤만 있음
   - 현재는 sklearn LogisticRegression baseline으로 대체 중 (ranking_baseline.joblib)
   - AUC ≥ 0.70 달성 여부 미측정

5. 세션 인코더 미구현
   - session/session_encoder.py 스켈레톤만 있음
   - 현재 세션 관심사는 Redis에서 단순 키워드 매칭으로 처리 중
   - GRU/Transformer 기반 단기 관심 임베딩 없음

6. 추천 성능 지표 미측정
   - HitRate@50, NDCG@50, Coverage 전부 미측정

────────────────────────────
[시뮬레이터]
────────────────────────────
7. simulator/ 폴더 자체 없음
   - 6가지 페르소나 정의 필요
     (트렌드세터, 실용주의자, 가성비추구, 브랜드충성, 충동구매, 신중탐색)
   - search / view / cart / purchase 이벤트 생성 로직 필요
   - 생성 로그를 Redis에 실시간으로 기록하는 로직 필요
   - config 파일로 페르소나 비율, 전이 확률 변경 가능하게 구성

────────────────────────────
[대시보드]
────────────────────────────
8. evaluation/streamlit_app.py는 완전 구현되어 있음
   - 단, docker-compose.yml에 dashboard 컨테이너가 없어서 배포가 안 됨
   - docker-compose에 Streamlit 컨테이너 추가 필요 (포트 8501)
   - 현재 streamlit_app.py는 샘플 CSV 데이터 기반으로 작동 중
   - 실제 검색/추천 API와 연동하는 실시간 데이터 파이프 연결 여부 확인 필요

────────────────────────────
[인프라]
────────────────────────────
9. docker-compose.yml에 컨테이너 2개 누락
   - simulator 컨테이너 없음 (simulator/ 폴더 생성 후 추가 필요)
   - dashboard 컨테이너 없음 (evaluation/ 폴더 기반으로 추가 필요)

10. CT 파이프라인 폴더 없음 (ct_pipeline/)
    - 성능 지표 주기적 모니터링 스크립트 없음
    - 성능 저하 시 알림 출력 없음
    - 신규 로그 10,000건 누적 시 재학습 트리거 없음
    - 모델 버전 관리 로직 없음

11. 문서화 미완성
    - docs/ 폴더에 실험 결과 리포트 없음 (평가 지표 미측정이 선행 조건)
    - README.md에 아키텍처 다이어그램 없음
    - 팀원별 기여도 명시 필요 (동료 평가 항목)

────────────────────────────
[이미 구현되어 있어 착각하기 쉬운 것들]
────────────────────────────
- MAB (Epsilon-Greedy) → rerank_bridge.py에 구현 완료
- Re-ranking 다양성 제어 (동일 카테고리 연속 3개 제한) → 구현 완료
- 신규 상품 노출 보장 로직 → 구현 완료
- A/B 테스트 통계 검정 (p-value, 신뢰구간) → evaluation/ab_test.py 구현 완료
- 오프라인 평가 메트릭 (HitRate, MRR, NDCG) → evaluation/metrics.py 구현 완료
- 프론트엔드 React 앱 → pront/ 폴더에 완전 구현 (docker-compose 미포함)
