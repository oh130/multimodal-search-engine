"""
추천 모델 공통 설정 로더.

역할:
- embedding_dim, batch_size, learning_rate, top_k 같은 공통 설정을 관리한다.
- 학습/평가/추론 환경에서 공통으로 참조하는 하이퍼파라미터 진입점이 된다.

작성 지침:
- 하드코딩된 숫자를 여러 파일에 흩뿌리지 말고 이 파일에서 모아 관리한다.
- candidate, ranking, serving이 함께 쓰는 설정과 개별 모듈 전용 설정을 구분한다.
- 환경별 설정 확장이 필요하면 파일 분리보다 로딩 규칙을 먼저 정의한다.
"""
